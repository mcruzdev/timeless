name: Timeless CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  timeless-api:
    name: Build & Test - Timeless API (Java)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./timeless-api        

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: timeless-api/src/main/webui/package-lock.json

      - name: Setup Chrome
        id: chrome
        uses: browser-actions/setup-chrome@v1

      - name: Install Frontend Dependencies
        working-directory: ./timeless-api/src/main/webui
        run: npm ci

      - name: Check code formatting with Prettier
        working-directory: ./timeless-api/src/main/webui
        run: npm run prettier:check

      - name: Ensure Maven wrapper is executable
        run: chmod +x mvnw

      - name: Build with unit and integration tests
        run: ./mvnw -B -ntp formatter:validate impsort:check package
        env:
          CHROME_BIN: ${{ steps.chrome.outputs.chrome-path }}

      - name: Setup Pack CLI
        uses: buildpacks/github-actions/setup-pack@v5.0.0

      - name: Login to Quay.io
        uses: docker/login-action@v3
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_PASSWORD }}

      - name: Build and Publish Timeless API Image
        run: |
          pack build quay.io/timeless/timeless-api:${{ github.sha }} \
            --path ./timeless-api \
            --builder paketobuildpacks/builder-jammy-base \
            --publish

  whatsapp:
    name: Build & Publish - WhatsApp
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./whatsapp

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: whatsapp/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Check code formatting with Prettier
        run: npm run prettier:check

      - name: Setup Pack CLI
        uses: buildpacks/github-actions/setup-pack@v5.0.0

      - name: Login to Quay.io
        uses: docker/login-action@v3
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_PASSWORD }}

      - name: Build and Publish WhatsApp Image
        run: |
          pack build quay.io/timeless/whatsapp:${{ github.sha }} \
            --path ./whatsapp \
            --builder paketobuildpacks/builder-jammy-base \
            --buildpack paketo-community/apt \
            --buildpack paketo-buildpacks/nodejs \
            --publish

